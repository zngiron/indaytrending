#import './Category.fragment.graphql'
#import './Post.fragment.graphql'

query Post($slug: ID!) {
  post(id: $slug, idType: SLUG) {
    ...Post
    content
    next {
      slug
    }
  }
  categories {
    edges {
      node {
        ...Category
      }
    }
  }
}