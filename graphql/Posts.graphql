#import './Post.fragment.graphql'

query Posts ($first: Int) {
  posts(first: $first, where: {
    dateQuery: {
      after: {
        year: 2019
        month: 1
      }
    },
    status: PUBLISH
  }) {
    edges {
      node {
        ...Post
      }
    }
  }
}